import React from 'react';
import Footer from './Footer';

const FooterContainer = ({currentScreenWidth}) => {
    return <Footer currentScreenWidth={currentScreenWidth}/>
};

export default FooterContainer;